<form method="post" action="{{ path('app_membro_delete', {'id': membro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ membro.id) }}">
    <button class="btn">Delete</button>
</form>
