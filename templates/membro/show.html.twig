{% extends 'base.html.twig' %}

{% block title %}Membro{% endblock %}

{% block body %}
    <h1>Membro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membro.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ membro.Nome }}</td>
            </tr>
            <tr>
                <th>Cpf</th>
                <td>{{ membro.cpf }}</td>
            </tr>
            <tr>
                <th>Nascimento</th>
                <td>{{ membro.nascimento ? membro.nascimento|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ membro.email }}</td>
            </tr>
            <tr>
                <th>Telefone</th>
                <td>{{ membro.telefone }}</td>
            </tr>
            <tr>
                <th>Logradouro</th>
                <td>{{ membro.logradouro }}</td>
            </tr>
            <tr>
                <th>Cidade</th>
                <td>{{ membro.cidade }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ membro.estado }}</td>
            </tr>
            <tr>
                <th>Igreja_pertencente</th>
                <td>{{ membro.igrejaPertencente }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_membro_index') }}">back to list</a>

    <a href="{{ path('app_membro_edit', {'id': membro.id}) }}">edit</a>

    {{ include('membro/_delete_form.html.twig') }}
{% endblock %}
