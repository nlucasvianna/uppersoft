<form method="post" action="{{ path('app_igreja_delete', {'id': igreja.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ igreja.id) }}">
    <button class="btn">Delete</button>
</form>
