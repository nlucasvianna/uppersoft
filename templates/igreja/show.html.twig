{% extends 'base.html.twig' %}

{% block title %}Igreja{% endblock %}

{% block body %}
    <h1>Igreja</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ igreja.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ igreja.nome }}</td>
            </tr>
            <tr>
                <th>Endereco</th>
                <td>{{ igreja.endereco }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ igreja.website }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_igreja_index') }}">back to list</a>

    <a href="{{ path('app_igreja_edit', {'id': igreja.id}) }}">edit</a>

    {{ include('igreja/_delete_form.html.twig') }}
{% endblock %}
